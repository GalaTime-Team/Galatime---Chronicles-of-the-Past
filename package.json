{
  "name": "galatime-chronicles-of-the-past",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace desktop",
    "dev:desktop": "npm run dev:desktop --workspace desktop",
    "build": "npm run build --workspace desktop",
    "build:desktop": "npm run build:desktop --workspace desktop",
    "test": "npm run test --workspace desktop"
  },
  "dependencies": {
    "@floating-ui/react": "^0.27.19"
  }
}
