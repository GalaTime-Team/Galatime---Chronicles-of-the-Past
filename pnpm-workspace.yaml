packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  # esbuild's postinstall provisions its platform binary.
  esbuild: true
  # tsParticles ships prebuilt bundles and needs no install scripts.
  '@tsparticles/engine': false
